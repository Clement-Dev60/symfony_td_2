<form method="post" action="{{ path('app_listing_delete', {'id': listing.id}) }}" onsubmit="return confirm('Supprimer cette annonce ?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ listing.id) }}">
    <button class="btn btn-danger">Supprimer</button>
</form>